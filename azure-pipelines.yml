# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- '*'

pool:
  vmImage: 'windows-latest'

steps:
- task: gitmirror@0
  inputs:
    GitRepoUrl: 'https://ivanaimufua41@dev.azure.com/ivanaimufua41/test-typescript-endpoint/_git/test-typescript-endpoint'
  displayName: 'Connect to git repo'